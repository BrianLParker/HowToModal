@page "/"

<h1>How to Modal</h1>

I would like to share with the community how to modal without javascript.
<hr />

<button @onclick="()=>modal1.ShowModal()" class="btn btn-primary">Modal 1</button>
<button @onclick="()=>modal2.ShowModal()" class="btn btn-primary">Modal 2</button>
<button @onclick="()=>modal3.ShowModal()" class="btn btn-primary">Modal 3</button>

<hr />

<StackFlare />
<br />
<em>Enjoy!</em>


<Modal @ref="@modal1" OnClose="Modal1Closed" Background="#0088ff77" BlurPixels="3" >
    <MyModalContent />
</Modal>

<Modal @ref="@modal2" AllowBackgroundClick="false">
    <div class="modal-content shadow-lg fadeIn">
        <div class="modal-header">
            <h5 class="modal-title">Weather Forecast</h5>
            <ModalCloseButton type="button" class="close" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </ModalCloseButton>
        </div>
        <div class="modal-body">
            <FetchData />
        </div>
        <div class="modal-footer">
            <div class="text-info"><small>This modal will not close by clicking outside the modal</small></div>
            <ModalCloseButton type="button" class="btn btn-secondary" aria-label="Close">Close</ModalCloseButton>
        </div>
    </div>
</Modal>

<Modal @ref="@modal3" Background="#0000ff77" BlurPixels="3">
    <BounceDemo BoxColor="#00ff00" BallColor="#ff00ff" />
</Modal>

